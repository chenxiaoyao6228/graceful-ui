import { Meta, Story, Props, Preview } from "@storybook/addon-docs/blocks";

import Icon from "../src/components/icon/icon.vue";

<Meta title="通用|Icon 图标" component={Icon} />

# Icon

### 引入组件

```js
import { Icon } from "graceful-ui";
```

### 类型

这里只展示组件库中所有组件中用到的图标, 可以点击展开代码复制需要的icon组件
<Preview>
  <Story name="type 类型">
    {{
      components: { Icon },
      template: `
        <div style="font-size: 24px">
          <Icon type="download" />
          <Icon type="upload" />
          <Icon type="setting" />
          <Icon type="loading" />
          <Icon type="success" />
          <Icon type="info" />
          <Icon type="error" />
          <Icon type="warning" />
          <Icon type="clear" />
          <Icon type="thumbs-down" />
          <Icon type="thumbs-up" />
          <Icon type="arrow-up" />
          <Icon type="arrow-down" />
          <Icon type="arrow-left" />
          <Icon type="arrow-right" />
          <Icon type="double-arrow-left" />
          <Icon type="double-arrow-right" />
          <Icon type="cross" />
          <Icon type="dots" />
          <Icon type="contact-person" />
          <Icon type="eye" />
          <Icon type="eye-closed" />
          <Icon type="search" />
          <Icon type="plus-without-circle" />
          <Icon type="pin" />
          <Icon type="retry" />
        </div>`,
    }}
  </Story>
</Preview>

## API

<Props of={Icon} />
